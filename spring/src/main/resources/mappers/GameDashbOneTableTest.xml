<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.spring.mapper.GameDashbOneTableTestMapper">

    <resultMap id="GameDashbOneTableTestEntityResultMap" type="com.ssafy.spring.entity.GameDashbOneTableTestEntity">
        <id column="game_id" property="gameId"/>
        <result column="title" property="title"/>
        <result column="short_description" property="shortDescription"/>
        <result column="long_description" property="longDescription"/>
        <result column="image_path" property="imagePath" typeHandler="com.ssafy.spring.entity.typehandler.StringArrayTypeHandler"/>
        <result column="description_video_path" property="descriptionVideoPath"/>
        <result column="game_profile_path" property="gameProfilePath"/>
        <result column="people" property="people" typeHandler="com.ssafy.spring.entity.typehandler.IntArrayTypeHandler"/>
        <result column="level" property="level"/>
    </resultMap>

    <select id="getGameListTest" resultMap="GameDashbOneTableTestEntityResultMap">
<!--        SELECT-->
<!--            game_id,-->
<!--            title,-->
<!--            short_description,-->
<!--            game_profile_path,-->
<!--            people,-->
<!--            level-->
<!--        FROM-->
<!--            game-->
        SELECT * FROM get_game_list()
    </select>

    <select id="getFilteredGameList" resultMap="GameDashbOneTableTestEntityResultMap">
        SELECT
            game_id,
            title,
            short_description,
            game_profile_path,
            people,
            level
        FROM
            game
        WHERE
            people @>
            <foreach collection="people" item="person" open="ARRAY[" close="]" separator=",">
                #{person}
            </foreach>
            AND level IN
            <foreach collection="level" item="lvl" open="(" close=")" separator=",">
                #{lvl}
            </foreach>
    </select>

    <select id="getGameDetailRule" resultMap="GameDashbOneTableTestEntityResultMap">
        SELECT
            game_id,
            game_profile_path,
            long_description,
            image_path,
            description_video_path
        FROM
            game
        WHERE
            game_id = #{gameId}
    </select>

    <select id="getSearchedGameList" resultMap="GameDashbOneTableTestEntityResultMap">
        SELECT
            game_id,
            title,
            short_description,
            game_profile_path,
            people,
            level
        FROM
            game
        WHERE
            title LIKE CONCAT('%', #{title}, '%')
    </select>

</mapper>
